Default rule files for smartdns-plus-ui.

These files are example templates used for first-time deployment.
You can edit, replace, or delete them later in WebUI or on disk.

Included files:
- no_ipv6.txt: example domains that should not return IPv6 answers
- local-ptr.txt: example local PTR / local service related domains
- direct.txt: example domains that should go direct
